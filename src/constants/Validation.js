// Default Dimensions
export const DEFAULT_CANVAS = {
  WIDTH: 800,
  HEIGHT: 600,
};

export const DEFAULT_WALL = {
  WIDTH: 5, // meters
  HEIGHT: 3, // meters
};

// Minimum Values
export const MIN_WALL = {
  WIDTH: 1, // meters
  HEIGHT: 1, // meters
};

export const MIN_STEP = {
  WALL: 0.5, // meters
};

// Canvas Display
export const BASE_WALL_DISPLAY = {
  WIDTH: 5, // base display width in meters
  HEIGHT: 3, // base display height in meters
};

// Conversion
export const CONVERSION = {
  MM_TO_M: 1000,
};

// Power Rounding
export const POWER_ROUND_STEP = 500; // Round power to nearest 500W
